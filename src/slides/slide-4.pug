section
  h2 Rendering
  section
    pre
        code.javascript.
            ReactDOM.render(&lt;Hi/&gt;, document.querySelector('#root'));
    p Babel will compile this code to:
    pre
        code.javascript.
            ReactDOM.render(
                React.createElement(Hi),
                document.querySelector('#root')
            );
    